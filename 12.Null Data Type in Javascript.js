//After creating the variable we can assign the value as NULL or null to the variable
let firstName=null;
console.log(firstName,typeof(firstName));//But it give the type of operator as object and not as null